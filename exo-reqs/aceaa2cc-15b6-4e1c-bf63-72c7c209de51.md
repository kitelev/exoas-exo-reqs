---
exo__Asset_uid: aceaa2cc-15b6-4e1c-bf63-72c7c209de51
exo__Asset_createdAt: 2026-08-16T21:05:00
exo__Instance_class:
  - "[[8c5af681-3413-4219-8636-0ac229d1b253|req__Requirement]]"
exo__Asset_createdBy: "[[4ef3962d-b8a7-42b5-bd28-88ec846f1d13]]"
exo__Asset_label: "req(exo): W3C-prefixed labels emit canonical vocabulary IRIs, not exocortex.my ad-hoc IRIs"
exo__Asset_isDefinedBy: "[[a64ca05b-ed45-4fbc-a8a9-54f9cfcf895c]]"
req__Requirement_status: "[[4bd932c2-2507-4a2d-b3f2-163e096bfa81|req__RequirementStatusApproved]]"
req__Requirement_priority: "[[481c3be1-d05c-4b78-8a3c-c61308d40bf1|req__RequirementPriorityP2]]"
req__Requirement_bindingClass: "[[f8841786-64c2-42a9-8b45-2d33fd6be87c|req__RequirementBindingClassIntegration]]"
req__Requirement_author: "[[de20a3f1-7483-4714-ab28-b45f5cf02c76|ExoAssistant]]"
req__Requirement_covers:
  - "Namespace.forPrefix registers the five external W3C vocabulary prefixes that the codebase ALREADY carries canonical IRI constants for (rdf, rdfs, owl, xsd) plus shacl (sh, a NEW constant http://www.w3.org/ns/shacl#), so that a vault asset whose exo__Asset_label parses as <w3c-prefix>__<LocalName> emits the CANONICAL vocabulary IRI instead of the ad-hoc fallback https://exocortex.my/ontology/<prefix>#<LocalName>. WHY: the graph carries each W3C term under TWO different IRIs -- as a PREDICATE it is already canonical (RDFVocabularyMapper hard-maps exo__Class_superClass -> Namespace.RDFS.term('subClassOf'); measured 2026-08-16: 319 uses in vault-my, 293 in vault-tbank, 378 in vault-exodev), while the ASSET that DEFINES that same term (label rdfs__subClassOf) emitted https://exocortex.my/ontology/rdfs#subClassOf. Definition and usage therefore never join. SCOPE OF THE CHANGE (both directions -- this is the load-bearing part): registering a vocabulary FORWARD-ONLY makes the emitter produce IRIs its own INVERSE cannot read. The inverses (iriToObsidianName, symbolicIriToPropertyKey) were anchored to the exocortex.my base, so this requirement ALSO makes them derive from the same KNOWN_NAMESPACES array via a shared Namespace.fromTermIRI -- otherwise de-reification of a W3C-prefixed predicate-def silently returns undefined, re-opening ems__Bug f68bf750 / #3904. That case is live, not hypothetical: exo__Statement_predicate -> owl#sameAs exists in all three vaults. The round-trip invariant symbolicIriToPropertyKey(labelToSymbolicIRI(k)) === k is asserted over EVERY registered namespace, enumerated from Namespace.knownNamespaces() rather than a literal list, so a vocabulary added later is covered automatically. CROSS-PACKAGE: the CLI validator's NAMESPACE_PREFIX_MAP already resolved rdf/rdfs/owl canonically (so core and CLI were ALREADY divergent for those three and this change ALIGNS them), but lacked xsd/sh -- those two entries are added here, else labelToOntologyIRI builds subClassOf edges under a different IRI than the converter emits and isSubClassOf yields false sh:class violations. MEASURED blast radius (all three canonical vaults, 2026-08-16): 26 / 25 / 26 assets carry W3C-prefixed labels in vault-my / vault-tbank / vault-exodev; no Literal in any of them quotes the ad-hoc form (the 61 / 59 / 63 ad-hoc matches are IRI-form objects, which is exactly what changes). A second emission surface exists: exo__Asset_aliases runs through the same expandClassValue path (19 alias triples per vault). ShaclLiteValidator already allowlisted the canonical and ad-hoc forms for rdf/rdfs/owl/xsd; sh was absent in BOTH forms and its canonical base is added here. Every exocortex.my prefix and every unregistered user prefix (aiKnow__ etc.) stays byte-identical. NON-GOALS (explicitly deferred): reading exo__Ontology_url at emission time -- measured as 1 external Ontology_url out of 138 in vault-my, and that one ($w3c -> https://www.w3.org/) is a site root, not a usable namespace base, so the data does not exist yet; it also turns Namespace.forPrefix from a pure string function into a vault-dependent one. Splitting exoas-w3c-aggregated into per-vocab ontologies is vault DATA (task 46613e8e). Registering vocabularies with zero vault presence (skos, dc, foaf) is hardcoding ahead of need."
exo__Asset_updatedAt: 2026-08-16T23:04:02
req__Requirement_approvedBy: "[[0aa339bc-9b56-400a-8148-cbde57bbf0b6|a.kitelev]]"
req__Requirement_approvedAt: 2026-08-16T21:10:00
---

# req(exo): W3C-prefixed labels emit canonical vocabulary IRIs, not exocortex.my ad-hoc IRIs

## Job Story

**When** I open the vault asset that defines a W3C vocabulary term (e.g. the `exo__Class` whose label is `rdfs__subClassOf`),
**I want** the graph to describe it under the same IRI that the engine already uses when that term appears as a predicate (`http://www.w3.org/2000/01/rdf-schema#subClassOf`),
**so that** a query about that term reaches BOTH its 319 usages and its definition, instead of silently splitting them across two IRIs that never join.

## Gherkin

```gherkin
Feature: canonical IRIs for external W3C vocabulary prefixes

  Background:
    Given the engine already holds canonical Namespace constants for rdf, rdfs, owl and xsd
    And RDFVocabularyMapper already emits predicates under those canonical IRIs

  Scenario: an rdfs-prefixed label emits the canonical RDFS IRI
    Given a vault asset whose exo__Asset_label is "rdfs__subClassOf"
    When the converter derives the symbolic IRI for that label
    Then the emitted IRI is "http://www.w3.org/2000/01/rdf-schema#subClassOf"
    And it is NOT "https://exocortex.my/ontology/rdfs#subClassOf"
    And it is byte-identical to the IRI the engine already uses for that predicate

  Scenario: the shacl prefix resolves to the canonical SHACL namespace
    Given a vault asset whose exo__Asset_label is "sh__severity"
    When the converter derives the symbolic IRI for that label
    Then the emitted IRI is "http://www.w3.org/ns/shacl#severity"

  Scenario Outline: every registered W3C prefix resolves canonically
    When Namespace.forPrefix is asked for "<prefix>"
    Then it returns the canonical namespace whose IRI is "<iri>"

    Examples:
      | prefix | iri                                           |
      | rdf    | http://www.w3.org/1999/02/22-rdf-syntax-ns#   |
      | rdfs   | http://www.w3.org/2000/01/rdf-schema#         |
      | owl    | http://www.w3.org/2002/07/owl#                |
      | xsd    | http://www.w3.org/2001/XMLSchema#             |
      | sh     | http://www.w3.org/ns/shacl#                   |

  Scenario: exocortex.my prefixes are unchanged (backward compatibility)
    Given a vault asset whose exo__Asset_label is "ems__Task"
    When the converter derives the symbolic IRI for that label
    Then the emitted IRI is still "https://exocortex.my/ontology/ems#Task"

  Scenario: an unknown prefix still falls back to the ad-hoc convention
    Given a frontmatter key "aiKnow__Memory_aboutConcept"
    When Namespace.fromPropertyKey parses it
    Then the namespace IRI is "https://exocortex.my/ontology/aiKnow#"
    And no code change is required to introduce a further user namespace
```

## Acceptance

- `Namespace.forPrefix("rdfs")` returns the canonical `Namespace.RDFS` singleton (reference equality), not a freshly constructed ad-hoc namespace.
- A new `Namespace.SHACL` constant exists with IRI `http://www.w3.org/ns/shacl#` and prefix `sh`.
- The full `packages/core` suite stays green — measured on the probe: **374/374 suites, 8599 tests**, zero regressions attributable to the change.
- Revert-verify: removing the W3C prefixes from `KNOWN_NAMESPACES` reddens the canonical-emission scenarios and leaves the exocortex.my backward-compatibility scenario green (non-vacuity: the delta is the W3C registration, not namespace resolution as a whole).

## Non-goals

- Deriving namespace bases from `exo__Ontology_url` at emission time (see `covers` — the data does not exist yet and the change is disproportionate today).
- Splitting `exoas-w3c-aggregated` into per-vocabulary ontologies. That is vault DATA and is tracked separately; this requirement is the engine half that makes such a split meaningful.
- Adding vocabularies with zero vault presence (e.g. `skos`, `dc`, `foaf`). `ShaclLiteValidator` already exempts them from range checks; registering them here without a single asset would be hardcoding ahead of need.
