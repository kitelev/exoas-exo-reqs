---
exo__Asset_uid: a38ac95b-b347-42e4-8522-f481ab422337
exo__Asset_isDefinedBy: "[[a64ca05b-ed45-4fbc-a8a9-54f9cfcf895c]]"
exo__Asset_createdAt: 2026-06-28T17:16:35+05:00
exo__Asset_updatedAt: 2026-06-28T18:46:29+05:00
exo__Asset_createdBy: "[[de20a3f1-7483-4714-ab28-b45f5cf02c76|ExoAssistant]]"
exo__Instance_class:
  - "[[8c5af681-3413-4219-8636-0ac229d1b253|req__Requirement]]"
exo__Asset_label: "req(exo): the pn__DailyNote read-view Layout renders up to three class-partitioned daily-efforts blocks — Actions (ems__Action), Tasks (the day's efforts minus Action/Project, meetings included), Projects (ems__Project) — each with an independent show/hide visibility flag resolved as frontmatter-override > Layout-default > built-in default (Actions+Tasks shown, Projects hidden), identically on desktop and mobile, configured in RDF/frontmatter rather than plugin settings"
req__Requirement_status: "[[fccf8fa4-8004-41ee-9102-595a588e9be7|req__RequirementStatusActive]]"
req__Requirement_priority: "[[481c3be1-d05c-4b78-8a3c-c61308d40bf1|req__RequirementPriorityP2]]"
req__Requirement_bindingClass:
  - "[[aefdb1a3-ae87-4d4e-93fb-fb989a243c0c|req__RequirementBindingClassComponent]]"
  - "[[cc677c5f-b4ac-4ec3-9baa-e7ab79b78113|req__RequirementBindingClassUnit]]"
req__Requirement_area: "[[bd76637d-5788-4c30-a3d8-c88dcdd9970f|Exocortex Development]]"
req__Requirement_author: "[[de20a3f1-7483-4714-ab28-b45f5cf02c76|ExoAssistant]]"
req__Requirement_covers:
  - "The pn__DailyNote read-view renders day-scoped effort sections, but their composition and visibility were hardcoded in the renderer — a user could not turn the Actions / Tasks / Projects sections on or off without editing TypeScript. Layout configuration in Exocortex is a homoiconic RDF concern (exo__Layout, RFC 9541965c): which blocks a daily note shows, and their default visibility, is user-configurable semantics and therefore belongs in the RDF Layout asset + per-note frontmatter override, not in plugin code or plugin settings (Homoiconicity Invariant, Q1=yes). This requirement makes the daily-note Layout render up to three INDEPENDENT class-partitioned blocks of the day's efforts — «Actions» (ems__Action instances of the day), «Tasks» (the day's efforts EXCEPT Action and Project — so meetings/ems__Meeting and any other Effort subclass stay here, RL#1 inclusive carve-out), «Projects» (ems__Project instances of the day) — each gated by an independent show/hide visibility flag. Visibility resolves with a fixed precedence: the note's frontmatter override wins over the exo__Layout asset's per-block default, which wins over the built-in default (Actions shown, Tasks shown, Projects HIDDEN — VL#3, a deliberate partial regression: Projects disappear from existing daily notes until explicitly enabled). The block-visibility flag and the daily-efforts-by-class block kind are NEW additions to the LayoutBlock model (Phase-0 de-risk confirmed ExoLayoutRenderer supports neither today). The daily note's special navigation sections (day nav) are preserved — rerouted through the block pipeline, not lost. The whole read-path uses Obsidian-core APIs via vault.adapter with no Platform gate, so it behaves identically on desktop and iPhone (Desktop↔Mobile Command Parity invariant). Prerequisite onto change: ems__Action becomes a real subclass of ems__Effort (RL#3) so the day's-efforts query and the Actions/Tasks partition are well-typed — with no regression to existing Effort-wide paths (blast radius ~8 Action instances)."
req__Requirement_verifiedBy:
  - "@req:a38ac95b-b347-42e4-8522-f481ab422337 bound across (Unit) packages/core/tests/domain/layout/blockVisibility.test.ts (coercion + precedence override>Layout-default>VL#3 + no-config VL#3 default), packages/core/tests/domain/layout/dailyEffortsPartition.test.ts (single-scan partition + RL#1 meeting-stays-in-Tasks carve-out + dual-IRI/UID-canon matching), packages/core/tests/domain/layout/LayoutBlock.test.ts (visible flag parse + daily-efforts-by-class kind), packages/core/tests/domain/constants/AssetClass.test.ts (ACTION = ems__Action); and (Component) packages/obsidian-plugin/tests/unit/presentation/renderers/ExoLayoutRenderer.dailyEfforts.test.ts (visible=false NOT rendered while sibling visible IS; partition into Actions/Tasks-carve-out/Projects; RL#1 meeting in Tasks at default; override>VL#3 precedence + string coercion; VL#3 no-config default) + packages/obsidian-plugin/tests/unit/presentation/renderers/UniversalLayoutRenderer.dailyEffortsSuppression.test.ts (legacy DailyTasksRenderer suppressed when daily-efforts layout active + daily nav preserved; back-compat: no daily-efforts layout → legacy runs). RL#3 ems__Action-subclass-of-ems__Effort verified by pre-existing TBox state (6a99d2ca superClass→086f71fa, structurally identical to clean ems__Task/Project). Each behavioral assertion revert-verified RED→GREEN (PR #3793): visibility gate, partition slice, RL#1 carve-out, override precedence, suppression gate — all type-preserving breaks confirmed RED then restored GREEN. Merged to exocortex main (squash 309a788e); CI required-14 + test-ui green. Desktop↔mobile parity + Homoiconicity structural (read-path vault.adapter only, config in RDF/frontmatter) — verified by code-reviewer (APPROVE, 0 CRITICAL/HIGH/MEDIUM). Live GUI smoke (desktop+iPhone via the deferred exo__Layout asset) = Phase 4-5 gate."
req__Requirement_implementedBy:
  - "PR #3793 (squash 309a788e). packages/core/src/domain/layout/LayoutBlock.ts — LayoutBlockBase.visible?: boolean (explicit exo__LayoutBlock_visible, undefined when absent) + new daily-efforts-by-class kind (class exo__DailyEffortsByClassBlock uid 22528ed6-6ec9-48d0-8e60-e370c0b242a9, partition actions|tasks|projects) + parser + type guards. packages/core/src/domain/layout/blockVisibility.ts — coerceVisibilityOverride + resolveBlockVisibility + resolveDailyEffortVisibility (override > Layout default > built-in VL#3) + BUILTIN_DAILY_EFFORTS_VISIBILITY. packages/core/src/domain/layout/dailyEffortsPartition.ts — single-scan partitionDailyEffortsByClass (RL#1 inclusive carve-out; dual-IRI/UID-canon matching). packages/core/src/domain/constants/AssetClass.ts — ACTION='ems__Action'. packages/obsidian-plugin/src/presentation/renderers/ExoLayoutRenderer.tsx — visibility gate (skip not-visible) + daily-efforts render via injected provider + once-per-render partition. packages/obsidian-plugin/src/presentation/components/LayoutBlocks.tsx — DailyEffortsBlockView. packages/obsidian-plugin/src/presentation/renderers/helpers/DailyEffortsCollector.ts — collectDayEfforts (vault.adapter, no Platform gate). packages/obsidian-plugin/src/presentation/renderers/UniversalLayoutRenderer.ts — suppress legacy DailyTasksRenderer when daily-efforts layout active (nav preserved); back-compat additive + dormant. RL#3 onto (ems__Action subclass) already present in TBox. The live-activating exo__Layout + 3 LayoutBlock vault assets for pn__DailyNote are DEFERRED to Phase 4-5 (temp-vault GUI smoke → rollout) per ui-smoke-transitive-proof + migration-pre-verify; spec captured in PR #3793 body + project hub."
---

# req(exo): pn__DailyNote Layout — independent show/hide of Actions / Tasks / Projects daily-efforts blocks (desktop + mobile, homoiconic)

## Job Story

When I open today's daily note, I want to turn the Actions, Tasks, and Projects sections on or off independently — set once as a sensible default for all daily notes, and override on a specific note when needed — without editing plugin code or digging in settings, and have it behave the same on my iPhone as on desktop.

## Statement (Gherkin)

```gherkin
Given a LayoutBlock with its visibility flag set to false
Then ExoLayoutRenderer does not render that block
And a sibling block with visibility true is rendered

Given a daily-efforts-by-class block on a pn__DailyNote
Then it selects the day's efforts (isEffortInDay) and partitions them by exo__Instance_class
And the «Actions» block shows ems__Action instances of the day
And the «Tasks» block shows the day's efforts EXCEPT Action and Project (so ems__Meeting and other Effort subclasses stay here — RL#1 inclusive carve-out)
And the «Projects» block shows ems__Project instances of the day
And the daily note's special navigation sections are still rendered (preserved through the block pipeline)

Given the default visibility (Tasks shown) and a day that has an ems__Meeting
Then the meeting still renders inside the «Tasks» block
# RL#1 — the primary falsifier of "0 regressions"

Given a per-note frontmatter visibility override AND an exo__Layout default AND the built-in default
Then the effective visibility resolves as: frontmatter override > exo__Layout default > built-in default

Given override values arriving as true / false / "true" / "false" / absent / null / a partial key set
Then each coerces deterministically to a boolean visibility with no unexpected block appearing or disappearing

Given a pn__DailyNote with NO visibility configuration anywhere
Then Actions is shown, Tasks is shown, and Projects is hidden
# VL#3 — out-of-the-box default

Given ems__Action declared as a subclass of ems__Effort
Then SHACL validation is clean
And existing Effort-wide queries/renderers show no regression

Given the daily-note Layout read-path
Then it uses only Obsidian-core APIs via vault.adapter with no Platform gate
And it renders identically on desktop and on mobile (Desktop↔Mobile Command Parity invariant)

Given block visibility is configured
Then it lives in the exo__Layout RDF asset and per-note frontmatter — never in plugin settings (Homoiconicity Invariant)
```

## Verification

**Planned binding (component + unit):** `@req:a38ac95b-b347-42e4-8522-f481ab422337` — finalized at impl (Phase 3). Unit tests on the extended `LayoutBlock` model + visibility-merge/coercion helper; component tests on `ExoLayoutRenderer` for visibility honoring, the daily-efforts-by-class partition (Actions / Tasks-carve-out / Projects), the RL#1 meeting-regression, precedence, coercion edge cases, the VL#3 no-config default, daily-nav preservation, and no-Platform-gate parity; SHACL for the ems__Action subclass. Each behavioral assertion revert-verified RED→GREEN. Status flips Active once the binding lands in exocortex main (per req-first SDD).

## Notes

- **Source:** RFC v3 `[[f5f178ae-d815-4c5c-9631-7248762aac8e|RFC: pn__DailyNote toggles]]` (Proposed); PMBOK project `[[b2bce264-f7fe-49b8-ba58-0147a761f40b|pn__DailyNote section toggles]]`.
- **Phase-0 de-risk (done):** `ExoLayoutRenderer` today supports neither per-block visibility nor a daily-efforts-by-class block → both are NEW model additions (RL#4); Layout replaces only the Asset-Relations section, so the daily special sections are rerouted, not replaced.
- **Decisions baked (locks):** VL#1 three independent toggles · VL#2 Layout-default + frontmatter-override merge · VL#3 default Actions+Tasks shown / Projects hidden · RL#1 Tasks inclusive carve-out (meetings stay) · RL#2 homoiconic exo__Layout · RL#3 ems__Action subclass of ems__Effort · RL#4 extend block model.
- **Priority P2:** broader than onboarding polish (renderer model extension + onto ripple), but reversible at review; Projects-hidden default is a deliberate accepted partial regression (R1, low practical impact — day-project blocks are usually empty).
- **Split note:** the code-child MAY carve the ems__Action-subclass onto change into its own req if its SHACL/ClassHierarchy test structure warrants; this req is the unified feature contract.
