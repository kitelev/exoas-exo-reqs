---
exo__Asset_uid: 740244c1-a29a-4c31-8d4d-c74b141fb80e
exo__Asset_isDefinedBy: "[[a64ca05b-ed45-4fbc-a8a9-54f9cfcf895c]]"
exo__Asset_createdAt: 2026-06-24T11:11:41+05:00
exo__Asset_updatedAt: 2026-06-24T11:11:41+05:00
exo__Asset_createdBy: "[[de20a3f1-7483-4714-ab28-b45f5cf02c76|ExoAssistant]]"
exo__Instance_class:
  - "[[8c5af681-3413-4219-8636-0ac229d1b253|req__Requirement]]"
exo__Asset_label: "req(exo): validate schema --shapes-mode resolves a person__Person-typed value as an ems__Agent subclass — no false sh:class violation (namespace-agnostic class resolution, #3512)"
req__Requirement_status: "[[fccf8fa4-8004-41ee-9102-595a588e9be7|req__RequirementStatusActive]]"
req__Requirement_priority: "[[01d50c3d-ade2-4b5f-a35c-f9fd120debd3|req__RequirementPriorityP0]]"
req__Requirement_bindingClass:
  - "[[f8841786-64c2-42a9-8b45-2d33fd6be87c|req__RequirementBindingClassIntegration]]"
req__Requirement_area: "[[bd76637d-5788-4c30-a3d8-c88dcdd9970f|Exocortex Development]]"
req__Requirement_author: "[[de20a3f1-7483-4714-ab28-b45f5cf02c76|ExoAssistant]]"
req__Requirement_covers:
  - "exo validate schema --shapes-mode — the SHACL-lite sh:class range gate over a property whose range is ems__Agent. Class membership is resolved namespace-agnostically (TripleClassHierarchy via labelToOntologyIRI): an instance typed by ANY well-formed exocortex.my namespace class (e.g. the canonical person__Person, a prefix absent from the curated NAMESPACE_PREFIX_MAP) resolves through its exo__Class_superClass chain to ems__Agent, so no false sh:class violation fires. Keeps EKA-canonical-prefix instances conformant without per-namespace curation (#3512)"
req__Requirement_verifiedBy:
  - "packages/cli/tests/integration/validate-schema-person-subclass.integration.test.ts::Issue #3512 — person__Person resolves as subclass of ems__Agent > does NOT emit a sh:class violation for a person__Person-typed responsible (KEY: fails pre-fix)"
req__Requirement_implementedBy:
  - "labelToOntologyIRI (packages/cli/src/commands/validate-schema.ts) — the namespace-agnostic fallback derives `https://exocortex.my/ontology/<prefix>#<Local>` for any well-formed lowercase prefix not in the curated NAMESPACE_PREFIX_MAP, so TripleClassHierarchy materialises the `person#Person → ems#Agent` subClassOf edge and isSubClassOf returns true for the sh:class check. CLI wiring: validate-schema runShapesValidation → TripleClassHierarchy → shaclValidate over the NoteToRDFConverter triple set"
---

# req(exo): validate schema --shapes-mode resolves a person__Person-typed value as an ems__Agent subclass — no false sh:class violation (namespace-agnostic class resolution, #3512)

## Job Story

When I consolidate my person instances onto the EKA-canonical `person__Person` class and run schema validation before committing, I want a `person__Person`-typed responsible on an `ems__Area` (whose `ems__Area_responsible` range is `ems__Agent`) to validate cleanly — exactly as the legacy deprecated `ims__Person` always did — so that the canonical-prefix migration does not produce a wall of false `sh:class` violations and block my commits.

## Statement (Gherkin)

```gherkin
Given an ems__Area whose ems__Area_responsible (range ems__Agent) points to an instance typed person__Person
And person__Person declares exo__Class_superClass ems__Agent
And the "person__" prefix is NOT present in the curated NAMESPACE_PREFIX_MAP
When I run `validate schema --shapes-mode` over the vault
Then NO sh:class violation is emitted for that responsible
And a legacy ims__Person-typed responsible also validates cleanly (no regression)
```

## Verification

**Revert-verified (integration binding):** `@req:740244c1-a29a-4c31-8d4d-c74b141fb80e` — gating the namespace-agnostic fallback in `labelToOntologyIRI` (`if (false && /^[a-z][a-zA-Z0-9]*$/.test(prefix) && ...)`, so the `person#Person → ems#Agent` ontology-IRI subClassOf edge is never materialised) makes `validate-schema-person-subclass.integration.test.ts "does NOT emit a sh:class violation for a person__Person-typed responsible (KEY: fails pre-fix)"` go **RED** (a false `sh:class` violation appears); restored → **GREEN**. The legacy `ims__Person` case (curated-map prefix) stays GREEN both ways — a precise revert (2026-06-24, al-reqmgmt-p2-cont, origin/main `79bee33b`).

> Migrated requirement (al-reqmgmt-p2-cont, P2 increment 7). Reverse-documented (A14) from an already-written test. The binding is `integration`-class (real `runShapesValidation` → `TripleClassHierarchy` → `shaclValidate` over a `NoteToRDFConverter`-built triple set on a production-shape TBox+ABox fixture), satisfying the P0 binding-class floor (RFC 0003 §3.6) — no deferred floor. Distinct SHACL behavior from the metaclass no-false-positive req (`60abc8ac`, which reverts hierarchy Pass-3 metaclass propagation) and the label-less-class req (`7a337cbd`, which reverts the validator's subclass-closure leg): this reverts the **namespace-agnostic IRI derivation** that materialises subClassOf edges for non-curated exocortex.my prefixes.
