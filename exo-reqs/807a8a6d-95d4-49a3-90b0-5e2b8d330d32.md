---
exo__Asset_uid: 807a8a6d-95d4-49a3-90b0-5e2b8d330d32
exo__Asset_isDefinedBy: "[[a64ca05b-ed45-4fbc-a8a9-54f9cfcf895c]]"
exo__Asset_createdAt: 2026-06-27T16:20:00+05:00
exo__Asset_updatedAt: 2026-06-27T16:35:47+05:00
exo__Asset_createdBy: "[[de20a3f1-7483-4714-ab28-b45f5cf02c76|ExoAssistant]]"
exo__Instance_class:
  - "[[8c5af681-3413-4219-8636-0ac229d1b253|req__Requirement]]"
exo__Asset_label: "req(exo): the plugin «Validate vault» command registers on BOTH desktop and mobile (no platform gate), reads the enabled-set from validation-check settings via the warm metadataCache, runs the check-runner, and reports the result"
req__Requirement_status: "[[fccf8fa4-8004-41ee-9102-595a588e9be7|req__RequirementStatusActive]]"
req__Requirement_priority: "[[01d50c3d-ade2-4b5f-a35c-f9fd120debd3|req__RequirementPriorityP0]]"
req__Requirement_bindingClass:
  - "[[cc677c5f-b4ac-4ec3-9baa-e7ab79b78113|req__RequirementBindingClassUnit]]"
req__Requirement_area: "[[bd76637d-5788-4c30-a3d8-c88dcdd9970f|Exocortex Development]]"
req__Requirement_author: "[[de20a3f1-7483-4714-ab28-b45f5cf02c76|ExoAssistant]]"
req__Requirement_covers:
  - "The Obsidian plugin «Exocortex: Validate vault» command (RFC f402002b, M1.5 plugin half — the core M1 value: validate on iPhone). Registered UNCONDITIONALLY on both desktop AND mobile (⛔ Desktop↔Mobile Command Parity invariant — NO Platform.isMobile gate; archgate MOBILE-004 enforces this). The command builds a PluginVaultCheckReader over the WARM metadataCache (vault.getMarkdownFiles() + metadataCache.getFileCache().frontmatter — never a per-file re-read, avoiding the iPhone-reindex-10-minutes anti-pattern), reads the enabled-set with the shared core readEnabledCheckIds over validation-check setting__Setting instances, runs VaultCheckRunner.runWithContext, and surfaces the per-check pass/fail/error report via the central notifier. The portable checks (uid-uniqueness, co-location) run on mobile with no platform machinery; SHACL/DAG runner thunks are M2-deferred (parity with the CLI's CliFsCheckReader) so enabling either is reported FAIL-LOUD, never a silent skip."
req__Requirement_verifiedBy:
  - "packages/obsidian-plugin/tests/unit/validation/registerValidationCommands.test.ts :: registerValidationCommands > registers «Validate vault» with the stable id and renders the report (the callback drives runValidation + notifies formatVaultCheckReport); packages/obsidian-plugin/tests/unit/validation/PluginVaultCheckReader.test.ts :: PluginVaultCheckReader > builds the warm asset array from the metadataCache source and omits the SHACL/DAG runners (M2-deferred → fail-loud)"
req__Requirement_implementedBy:
  - "registerValidateVaultCommand (registerValidationCommands.ts, id `validate-vault`) + PluginVaultCheckReader (PluginVaultCheckReader.ts) + the ExocortexPlugin.registerVaultValidationCommands() wiring (warm source from app.vault/metadataCache, readEnabledCheckIds, VaultCheckRunner). Registered unconditionally in onload — no platform gate."
---

# req(exo): plugin «Validate vault» on desktop AND mobile — warm reader + enabled-set + fail-loud

## Job Story

When I run «Exocortex: Validate vault» from the command palette — on my iPhone just as on desktop — I want it to run exactly the integrity checks I enabled (via validation-check `setting__Setting` instances), report each, and loudly error (never silently pass) on a check I enabled but whose runner is not wired — so vault-integrity validation is a first-class, cross-platform action and not a desktop-only / CLI-only capability.

## Statement (Gherkin)

```gherkin
Given the plugin is loaded (on desktop OR mobile)
Then the «Validate vault» command is registered with the stable id `validate-vault` on both platforms

Given a vault whose validation-check setting__Setting instances enable uid-uniqueness
When I run «Validate vault»
Then the warm reader builds the asset array from the metadataCache (no per-file re-read)
And VaultCheckRunner runs the enabled-set and the report is surfaced via a Notice

Given an enabled check whose runner is not wired (SHACL/DAG, M2-deferred)
When «Validate vault» runs
Then that check is reported as an ERROR (fail-loud), not a silent pass
```

## Verification

**Bound (unit/component):** `@req:807a8a6d-95d4-49a3-90b0-5e2b8d330d32` — the registrar test asserts the stable id/name + that the callback drives `runValidation` and notifies the formatted report; the reader test asserts the warm asset array is built from the metadataCache source and the SHACL/DAG runner thunks are omitted (M2-deferred → fail-loud). al-settings-m1.5-cont, 2026-06-27.
