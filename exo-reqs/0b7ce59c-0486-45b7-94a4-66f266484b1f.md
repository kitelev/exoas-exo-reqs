---
exo__Asset_uid: 0b7ce59c-0486-45b7-94a4-66f266484b1f
exo__Asset_isDefinedBy: "[[a64ca05b-ed45-4fbc-a8a9-54f9cfcf895c]]"
exo__Asset_createdAt: 2026-06-27T16:20:00+05:00
exo__Asset_updatedAt: 2026-06-27T16:20:00+05:00
exo__Asset_createdBy: "[[de20a3f1-7483-4714-ab28-b45f5cf02c76|ExoAssistant]]"
exo__Instance_class:
  - "[[8c5af681-3413-4219-8636-0ac229d1b253|req__Requirement]]"
exo__Asset_label: "req(exo): the plugin «Scaffold validation settings» command registers on BOTH desktop and mobile (no platform gate), opens an ontology picker, and writes the 4 validation-check settings (uid-uniqueness=true, rest=false) co-located in the chosen ontology via vault.adapter"
req__Requirement_status: "[[cb2e9a63-081e-46fa-89b9-7ed479516a62|req__RequirementStatusProposed]]"
req__Requirement_priority: "[[01d50c3d-ade2-4b5f-a35c-f9fd120debd3|req__RequirementPriorityP0]]"
req__Requirement_bindingClass:
  - "[[cc677c5f-b4ac-4ec3-9baa-e7ab79b78113|req__RequirementBindingClassUnit]]"
req__Requirement_area: "[[bd76637d-5788-4c30-a3d8-c88dcdd9970f|Exocortex Development]]"
req__Requirement_author: "[[de20a3f1-7483-4714-ab28-b45f5cf02c76|ExoAssistant]]"
req__Requirement_covers:
  - "The Obsidian plugin «Exocortex: Scaffold validation settings» command (RFC f402002b, M1.5 plugin half). Registered UNCONDITIONALLY on both desktop AND mobile (⛔ Desktop↔Mobile Command Parity — NO Platform.isMobile gate). The command opens an OntologyFuzzyModal listing the vault's ontologies (co-location anchors — the isDefinedBy targets resolvable in the vault; the picker uses the deferred-microtask resolution contract proven necessary by the picker-no-op bug #3561) and, on choice, writes the 4 validation-check setting__Setting instances with the M1 defaults (uid-uniqueness=true, co-location/shacl/dag=false) — each UID-named, exo__Instance_class setting__Setting, setting__Setting_key=[[check-id]], isDefinedBy=chosen ontology — co-located in that ontology's folder via vault.adapter.write (mobile-portable, no Node fs). This gives «Validate vault» an enabled-set to read. Mirrors the CLI `scaffold validation-settings` defaults byte-for-byte."
req__Requirement_verifiedBy:
  - "packages/obsidian-plugin/tests/unit/validation/registerValidationCommands.test.ts :: registerValidationCommands > registers «Scaffold validation settings» with the stable id and, on a picked ontology, scaffolds + notifies (cancel → no scaffold); packages/obsidian-plugin/tests/unit/validation/ValidationScaffolder.test.ts :: ValidationScaffolder > writes 4 check-settings co-located in the chosen ontology folder with exactly one (uid-uniqueness) true and isDefinedBy=ontology (changing the default breaks the trueCount===1 assertion)"
req__Requirement_implementedBy:
  - "registerScaffoldValidationCommand (registerValidationCommands.ts, id `scaffold-validation-settings`) + ValidationScaffolder (ValidationScaffolder.ts, SCAFFOLD_DEFAULTS + settingAsset template) + OntologyFuzzyModal (OntologyFuzzyModal.ts, deferred-resolution) + listOntologyCandidates (PluginVaultCheckReader.ts) + the ExocortexPlugin.registerVaultValidationCommands() wiring (vault.adapter writer, crypto.randomUUID). Registered unconditionally in onload — no platform gate."
---

# req(exo): plugin «Scaffold validation settings» on desktop AND mobile — ontology picker + 4 co-located settings

## Job Story

When I run «Exocortex: Scaffold validation settings» — on my iPhone just as on desktop — I want to pick an ontology and have the 4 validation-check settings created with safe defaults (only uid-uniqueness on) and physically co-located in that ontology's folder, so «Validate vault» immediately has an enabled-set to read and the config lives where I expect it.

## Statement (Gherkin)

```gherkin
Given the plugin is loaded (on desktop OR mobile)
Then the «Scaffold validation settings» command is registered with the stable id `scaffold-validation-settings` on both platforms

Given a vault with at least one ontology (co-location anchor)
When I run «Scaffold validation settings» and pick an ontology
Then 4 setting__Setting instances are written via vault.adapter into that ontology's folder
And exactly one (uid-uniqueness) has setting__Setting_value true, the other three false
And each references its check-key and isDefinedBy the chosen ontology

Given I dismiss the picker without choosing
Then nothing is written
```

## Verification

**Bound (unit/component):** `@req:0b7ce59c-0486-45b7-94a4-66f266484b1f` — the registrar test asserts the stable id/name + that picking an ontology scaffolds & notifies while cancel writes nothing; the scaffolder test asserts 4 files are written co-located in the chosen folder, exactly 1 with `setting__Setting_value: true` referencing the uid-uniqueness check-key, and isDefinedBy=ontology (changing the default breaks `trueCount===1`). al-settings-m1.5-cont, 2026-06-27.
