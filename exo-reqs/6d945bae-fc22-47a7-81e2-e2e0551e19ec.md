---
exo__Asset_uid: 6d945bae-fc22-47a7-81e2-e2e0551e19ec
exo__Asset_createdAt: 2026-09-20T13:28:35
exo__Asset_updatedAt: 2026-09-20T14:36:14
exo__Instance_class:
  - "[[8c5af681-3413-4219-8636-0ac229d1b253]]"
exo__Asset_createdBy: "[[4ef3962d-b8a7-42b5-bd28-88ec846f1d13]]"
exo__Asset_label: "req(exo): the duplicate exo__Property_range report is emitted per ADDRESSED property — a command writing property A never reports a conflicting twin of an unrelated property B, and writing B itself still yields exactly one line; ticket 3fc34b92, sibling of 21ceea14"
aliases:
  - "req(exo): the duplicate exo__Property_range report is emitted per ADDRESSED property — a command writing property A never reports a conflicting twin of an unrelated property B, and writing B itself still yields exactly one line; ticket 3fc34b92, sibling of 21ceea14"
exo__Asset_isDefinedBy: "[[a64ca05b-ed45-4fbc-a8a9-54f9cfcf895c]]"
req__Requirement_status: "[[fccf8fa4-8004-41ee-9102-595a588e9be7|req__RequirementStatusActive]]"
req__Requirement_priority: "[[481c3be1-d05c-4b78-8a3c-c61308d40bf1|req__RequirementPriorityP2]]"
req__Requirement_area: "[[bd76637d-5788-4c30-a3d8-c88dcdd9970f|Exocortex Development]]"
req__Requirement_author: "[[de20a3f1-7483-4714-ab28-b45f5cf02c76|ExoAssistant]]"
req__Requirement_bindingClass: "[[f8841786-64c2-42a9-8b45-2d33fd6be87c|req__RequirementBindingClassIntegration]]"
exo__Asset_relates:
  - "[[21ceea14-50dd-4cf8-bd3b-5a50b7c97105]]"
  - "[[3fc34b92-8fb3-445c-9ab2-4ada60077e37]]"
req__Requirement_covers:
  - "exo cli create / set-property — OBSERVABLE, stated by COMMAND: when the command writes a property X whose mounted TBox carries two or more definitions of X with DIFFERENT exo__Property_range, EXACTLY ONE `[PropertyNameValidator] property X is declared more than once …` line reaches stderr; when X is NOT among the properties the command writes, NO such line is written for it, however many conflicting duplicates the mounted TBox contains. The trigger is ADDRESSING the property, not collecting the TBox. 'The properties the command writes' means the FINAL set, INCLUDING the ones the CLI injects itself rather than the caller typing them — for `create` on a status-bearing class that is `ems__Effort_status` (an explicit --status, or the default Backlog). This clause separates 'the keys the caller typed' from 'the properties the command writes': the key check judges the first (the CLI's own keys are injected downstream and validating them there would be wrong), the diagnostic judges the second. Added 2026-09-20 after review round 1 of PR #4295 found the implementation had collapsed the two into one early snapshot, which dropped every injected property from the diagnostic — a regression against the pre-ticket behaviour, where collect() did report it."
  - "packages/cli/src/services/PropertyNameValidator.ts — collect() becomes pure data collection: a conflicting duplicate is RECORDED in the cached set keyed by property name (the FIRST conflicting twin in byte-ordered walk order supplies the message, as before) and collect() no longer calls the warn channel. The emission moves to declaredRange(name), which reports the recorded message for THAT name under a per-instance latch, so repeated addressing of the same name still yields exactly one line. Why the accessor and not a name filter inside collect(): collect() is CACHED — after the first call its loop never runs again, so an in-collect filter could only ever report for the first caller's name and would be silent for every later one on the same instance."
  - "packages/cli/src/services/PropertyNameValidator.ts — declaredRanges() takes an optional `addressed` iterable and reports for those names only; called without it (a pure bulk hand-off that claims no addressing) it is silent, and the returned map is unchanged and complete. packages/cli/src/commands/create.ts names the two sets SEPARATELY, because they are two concepts: `userPropertyKeys` (the raw keys the caller typed) goes to validate(), and `writtenProperties` — Object.keys(properties) read AFTER every injection, immediately before the hand-off — goes to declaredRanges(). Taking the final state rather than listing the known injections is what keeps the NEXT injection covered by construction."
  - "Unchanged, byte for byte: the message TEXT; first-wins resolution and the byte-ordered walk; 'identical twins are silent'; a definition with an EMPTY range still being skipped BEFORE first-wins (the in-code comment is corrected to say so — it previously read as if first-wins were the only rule); the warn channel DEFAULT (a no-op) and its wiring to stderr in create / set-property. This requirement narrows WHERE the report is emitted, never HOW it is delivered. isUnknownName / remove-property keep the default no-op channel and are untouched."
  - "Observable, measured on origin/main efc88f84 (2026-09-20). Defect reproduced by fixture at three levels — declaredRange('ems__Reminder_text') and validate(['ems__Reminder_text']) each emit one warning naming the UNRELATED ems__Other_twin, and the REAL setPropertyCommand() writing ems__Reminder_text prints that line to stderr while the write lands (WROTE=true). Live exposure with the PRODUCT collector over each canonical vault: conflicting duplicates 0 / 0 / 0 [vault-exodev/my/tbank] with the canary alive (known names 717 / 509 / 492, declared ranges 481 / 285 / 287) — the defect is LATENT, so the axes are built on a fixture, not on live data."
  - "Not governed: the quoted-label defect in ShapeLoader.registerCandidate that drops definitions before their domain is parsed (measured 2026-09-20: quoted exo__Asset_label among domain-carrying definitions 15 / 15 / 15 out of 697 / 492 / 474) — ticket efe993e1, a separate pre-existing defect, deliberately not fixed here. The SHACL surface is untouched (validate schema --shapes-mode goes through loadFromRDFGraph; baseline measured on this branch BEFORE the change: 9 / 0 / 0 violations, 546 / 364 / 459 warnings)."
  - "Relation to 21ceea14: that requirement stays ACTIVE and only ONE of its clauses is narrowed in place with a SUPERSEDED marker naming this requirement — its scenario 'When either command COLLECTS the declared ranges … ONE line is written to stderr', where collection is replaced by ADDRESSING as the trigger. Every other clause of 21ceea14 (declared-range typing, the reader oracle, supplied-key lookup, first-wins, identical-twin silence) is untouched and remains in force."
req__Requirement_approvedBy: "[[de20a3f1-7483-4714-ab28-b45f5cf02c76|ExoAssistant]]"
req__Requirement_approvedAt: 2026-09-20T13:30:57
req__Requirement_implementedBy:
  - "PR kitelev/exocortex#4295 (squash 2fdabf1c7727ada97e735b5d1179bb15f10393dd, CI 14/14 required green on head eb549d42, CodeQL 0, release v16.241.11 — the tag resolves to the merge sha exactly and npm dist-tags.latest = 16.241.11, both verified WITHOUT the GitHub API (git over ssh.github.com:443 + registry.npmjs.org); ticket 3fc34b92): packages/cli/src/services/PropertyNameValidator.ts — collect() records conflicting duplicates in the cached set keyed by property name (first conflicting twin in byte order supplies the message, text byte-identical) instead of reporting from the walk; the new private report() emits for ONE addressed name under a per-instance `reported` latch; declaredRange(name) reports that name, declaredRanges(addressed?) reports only the names the caller declares it writes and is silent without the argument. packages/cli/src/commands/create.ts — two separately named sets: userPropertyKeys (raw caller keys) → validate(), and writtenProperties = Object.keys(properties) read AFTER every injection, immediately before the hand-off → declaredRanges()."
  - "Review (ORCH, 2 rounds). r1 REQUEST-CHANGES, 1 HIGH: the first implementation used ONE shared snapshot for both call sites, taken BEFORE create injects ems__Effort_status (:474 explicit --status, :482 default Backlog) — so a conflict on an injected property was never reported, a REGRESSION against origin/main where collect() did report it. Confirmed by my own reading before fixing. Root was structural (two concepts, one variable), and the reviewer withdrew his own earlier 'one variable for both' instruction: validate() and declaredRanges() have different correct scopes. r2 MERGE after an independent replay on the head (write sites enumerated, anchors swept, required set re-read). LOW: the latch's unit of guarantee (per INSTANCE) is now stated at the field. NIT: the two guards without their own mutant have their transitive coverage named in the spec _doc."
  - "packages/cli/tests/integration/warn-scope-3fc34b92.integration.test.ts — N1 conflict-free name is silent about an unrelated twin · N2 the conflicting name reports once, a repeat addressing adds nothing (latch) · N3 the key check alone reports nothing · N4 set-property writing a conflict-free property says nothing about the twin and the write lands · N5 writing the conflicting property itself still gives exactly one line · N6 ORDERING: two properties written, only the second conflicting → one line naming the SECOND · N7 create writing only conflict-free properties is silent · N8 a bulk declaredRanges() naming nothing is silent and the map is complete · N9 the default no-op channel prints and throws nothing · N10 an INJECTED property (default Backlog status) counts as written and is reported once · N11 --no-status injects nothing and is silent. Suite 11/11; cli under CI=true 185/185 suites / 2197 tests; core 395/395 suites / 8955 tests."
  - "Mutants, whole set re-run in round 2 (integration-test-revert-verify §A18), control 0 red in every one: warn-scope-3fc34b92.validator.spec.json 7/7 — M1 first conflict no longer wins the message → [Q4]; M2 latch READ dropped → [N2]; M3 emission not keyed by the addressed name → [N1 N4 N6 N7 N10]; M4 declaredRanges never reports → [W7 N6 N8 N10]; M5 latch WRITE dropped → [N2]; M6 message no longer names the property → [Q4 S7 W7 N2 N5 N6 N8 N10]; M7 warn never called → [Q4 S7 W7 N2 N3 N5 N6 N8 N10]. warn-scope-3fc34b92.create.spec.json 2/2 — MC1 create names nothing → [W7 N6 N10]; MC2 the diagnostic takes the EARLY snapshot → [N10] and ONLY N10, which is what locks the review's HIGH. Sibling specs re-anchored and re-run because this change moved their subject's code (§A71): writer-string-oracle-8185c9dd.validator 6/6 (M8 / M9 re-pointed at the same two guarantees in their new home), writer-range-typing-2227d660.validator 4/4. spec-anchor-sweep over the whole repo: 31 specs parsed, 6 on touched subjects, 0 broken anchors."
  - "Live [vault-exodev/my/tbank] 2026-09-20: conflicting duplicates 0 / 0 / 0 measured with the PRODUCT collector (canary alive — names 717 / 509 / 492, declared ranges 481 / 285 / 287), so the defect is LATENT and the axes are fixture-based by necessity. validate schema --shapes-mode 9 / 0 / 0 violations and 546 / 364 / 459 warnings — identical before and after, measured on this branch both times. req__Requirement_activatedAt skipped — not declared in the exoas-req TBox."
---

# req(exo): the duplicate-range report names ONLY a property the command is actually writing

## Job Story

When I run `exocortex-cli set-property` or `create` on a vault whose mounted TBox happens to contain a duplicated property definition somewhere, I want the `⚠ [PropertyNameValidator] …` diagnostic to appear only when the duplicate concerns a property **I am writing**, so that the warning stays actionable — instead of reporting every conflicting twin in the whole mounted TBox on every single write, whatever property that write touches.

## Context — the defect, measured

`PropertyNameValidator.collect()` walks the mounted vault once and, for a property label declared by two or more definitions with DIFFERENT `exo__Property_range`, emits the report through the injected `warn` channel **inside that walk**. `collect()` is cached and is reached from the very first public call of the validator — `validate(keys)`, which both writers run for the key check before anything else. So the report fires at the first cache access, for **every** conflicting duplicate in the mounted TBox, regardless of which property the command is writing.

Reproduced on `origin/main` `efc88f84` (2026-09-20) with a fixture TBox mounting a single, conflict-free definition `ems__Reminder_text` and an unrelated pair of twins `ems__Other_twin` (`xsd:integer` vs `xsd:string`), at three levels:

- `declaredRange("ems__Reminder_text")` → 1 warning, and it names `ems__Other_twin`;
- `validate(["ems__Reminder_text"])` alone → the same 1 warning;
- the REAL `setPropertyCommand()` writing `--property ems__Reminder_text --value hello` → stderr carries `⚠ [PropertyNameValidator] property ems__Other_twin is declared more than once …` while the write itself lands (`WROTE=true`).

Live exposure, measured the same day with the PRODUCT collector over each canonical vault: conflicting duplicates **0 / 0 / 0** `[vault-exodev/my/tbank]` with the canary alive (known property names **717 / 509 / 492**, declared ranges **481 / 285 / 287**). The defect is therefore **latent**: the axes below are built on a fixture, not on live data.

## Design — why the emission moves to the ACCESS point

`collect()` cannot filter by the addressed name: it is cached, so after the first call its loop never runs again. A name filter inside it would report for the FIRST caller's name only and be silent for every later one, on the same validator instance. The only place that knows which name is being addressed is the public accessor.

So `collect()` becomes pure data collection — it records the conflicts it finds in the cached set, keyed by property name, with the message text unchanged — and `declaredRange(name)` emits the recorded message for THAT name, under a per-instance latch so that repeated addressing of the same name still yields exactly one line.

`declaredRanges()` (the plural form) hands the WHOLE map to `GenericAssetCreationService`; it does not itself address a name, because the service resolves per supplied key inside. It therefore stays silent unless the caller names what it addresses: it takes an optional `addressed` argument, and `create` passes the very same key set it already passes to `validate()`, held in ONE local variable so that "the same set" is a property of the construction rather than a coincidence of two expressions.

## Scenarios

```gherkin
Given a mounted TBox in which the property ems__Other_twin is declared twice with DIFFERENT exo__Property_range
And the property ems__Reminder_text is declared exactly once
When I run set-property (or create) writing ONLY ems__Reminder_text
Then the command succeeds
And NO ⚠ [PropertyNameValidator] line naming ems__Other_twin is written to stderr

Given the same mounted TBox
When I run set-property (or create) writing ems__Other_twin itself
Then EXACTLY ONE ⚠ [PropertyNameValidator] property ems__Other_twin … line is written to stderr
And the value is still typed by the FIRST definition in byte-ordered path walk

Given the same mounted TBox
When a command writes TWO properties and only the SECOND of them has conflicting definitions
Then exactly one line is written, and it names the SECOND property
# the emission follows the ADDRESSED name, not the first cache access

Given the same mounted TBox
When the same property name is addressed twice on one validator instance
Then still exactly ONE line is written
# the per-instance latch; collect() is cached and must not re-report

Given the same mounted TBox
When declaredRanges() is called WITHOUT naming any addressed property
Then no line is written at all, and the returned map is still complete
# a bulk hand-off claims no addressing

Given the same mounted TBox and NO warn channel injected
When any property is addressed, conflicting or not
Then nothing is printed and nothing is thrown
# the default channel stays a no-op — the DELIVERY mechanism is not changed, only the SCOPE of emission

Given a status-bearing class (an ems__Effort subclass) whose ems__Effort_status
  is declared twice with DIFFERENT ranges
When I run create on that class WITHOUT naming a status
Then the command injects the default Backlog status itself
And EXACTLY ONE line is written, and it names ems__Effort_status
# an injected property is a WRITTEN property; --no-status injects nothing and is silent
```

## Non-goals (named, not silent)

- The `warn` channel default (a no-op) and its wiring to stderr in `create` / `set-property` are unchanged — this requirement narrows WHERE the report is emitted, never HOW it is delivered.
- First-wins resolution, byte-ordered walk, the "identical twins are silent" rule and the message TEXT are unchanged, byte for byte.
- `isUnknownName` / `remove-property` keep the default no-op channel and are untouched.
- The quoted-label defect that drops 15 definitions per vault before their domain is parsed (ticket `efe993e1`) is a separate pre-existing defect and is deliberately not fixed here.

## Relation to req 21ceea14

Requirement `21ceea14` stays **Active**; only one of its clauses is narrowed in place with a `SUPERSEDED by <this requirement> (2026-09-20)` marker. The clause it supersedes is its scenario "When either command **collects** the declared ranges … ONE ⚠ [PropertyNameValidator] … line is written to stderr" — collection is replaced by ADDRESSING as the trigger. Every other clause of `21ceea14` (declared-range typing, the reader oracle, supplied-key lookup, first-wins, identical-twin silence) is untouched and remains in force.

